import { CheckoutPageContent } from '@/features/checkout/components/checkout-page-content';

export const metadata = {
  title: 'Checkout | PharmaNest BD',
};

export default function CheckoutPage() {
  return <CheckoutPageContent />;
}
