import { CartPageContent } from '@/features/cart/components/cart-page-content';

export const metadata = {
  title: 'Cart | PharmaNest BD',
};

export default function CartPage() {
  return <CartPageContent />;
}
