import { AccountAddressesContent } from '@/features/account/components/account-addresses-content';

export const metadata = { title: 'My Addresses | PharmaNest BD' };

export default function AddressesPage() {
  return <AccountAddressesContent />;
}
