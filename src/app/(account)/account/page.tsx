import { AccountDashboardContent } from '@/features/account/components/account-dashboard-content';

export const metadata = { title: 'My Account | PharmaNest BD' };

export default function AccountPage() {
  return <AccountDashboardContent />;
}
