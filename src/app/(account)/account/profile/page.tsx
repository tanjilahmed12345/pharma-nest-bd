import { AccountProfileContent } from '@/features/account/components/account-profile-content';

export const metadata = { title: 'Profile | PharmaNest BD' };

export default function ProfilePage() {
  return <AccountProfileContent />;
}
