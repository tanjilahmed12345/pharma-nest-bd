import { RegisterForm } from '@/features/auth/components/register-form';

export const metadata = { title: 'Register | PharmaNest BD' };

export default function RegisterPage() {
  return <RegisterForm />;
}
